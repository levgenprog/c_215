defmodule Pratie do
# bread = 10
# breadC = 10
# milk = 3
# milkC = 200
# cake = 1
# cakeC = 1500
def clc(bread, breadC, milk, milkC, cake, cakeC) do
  result = (bread * breadC + milk * milkC + cake * cakeC) / 100
end
def travel() do
  dist = 200
  time = 4
  velocity = dist / time

end
end
